/*"use client";

// Importa js-cookie en un componente de React
import Cookies from 'js-cookie';

// Ejemplo de cómo establecer una cookie en un componente de React
const setCookie = () => {
  // Establece una cookie llamada 'tokenLogin' con un valor 'mi_token'
  Cookies.set('tokenLogin', 'mi_token', { expires: 30 });
}

// Ejemplo de cómo obtener una cookie en un componente de React
const getCookie = () => {
  // Obtiene el valor de la cookie 'tokenLogin'
  const token = Cookies.get('tokenLogin');
  console.log(token); // Imprime el valor de la cookie en la consola
}

module.exports = { getCookie };*/